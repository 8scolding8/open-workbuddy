# IMA Remote MCP

Read-only remote MCP bridge for Tencent IMA OpenAPI. Secrets are supplied only through deployment environment variables.

Set `IMA_SMOKE_TEST=1` temporarily to run the non-content-leaking end-to-end startup smoke test.
